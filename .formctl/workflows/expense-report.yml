name: expense-report
url: http://127.0.0.1:4173/expense
safety:
  dryRunFirst: true
  approvalRequired: true
  selectorDrift: fail
  fileInputs: redacted
fields:
  - name: amount
    selector: input[name="amount"]
    type: number
  - name: receipt
    selector: input[name="receipt"]
    type: file
submit:
  selector: button[type="submit"]
